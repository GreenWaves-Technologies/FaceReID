<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light">
        <item name="android:actionBarStyle">@style/AppTheme.ActionBar</item>
    </style>

    <!-- ActionBar styles -->
    <style name="AppTheme.ActionBar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid">
        <item name="android:background">@color/white_color</item>
        <item name="android:backgroundStacked">@color/gwt_green_color</item>
        <item name="android:actionMenuTextColor">@color/black_text</item>
    </style>

</resources>
